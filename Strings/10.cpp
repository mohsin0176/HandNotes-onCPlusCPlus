#include<iostream>
#include<bits/stdc++.h>
#include<math.h>
#include<cstdlib.h>
#include<limits.h>
#include<iomanip.h>
#include<fstream.h>
#include<process.h>
#define PI 3.1416
using namespace std;

const int max=50;

int main()
{
     char str[max];
     int i,len;
     gets(str);
 	for (int i = 0; str[i] !='\0'; ++i)
 	{
 	len=i;
 	}
     cout<<len;
	return 0;
}